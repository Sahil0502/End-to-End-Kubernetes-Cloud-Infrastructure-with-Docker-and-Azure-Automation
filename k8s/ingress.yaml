apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: k8s-azure-app-ingress
  namespace: default
  labels:
    app: k8s-azure-app
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
  - hosts:
    - your-domain.com
    secretName: k8s-azure-app-tls
  rules:
  - host: your-domain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: k8s-azure-app-service
            port:
              number: 80

---
# Alternative Application Gateway Ingress Controller (AGIC) configuration
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: k8s-azure-app-agic-ingress
#   namespace: default
#   annotations:
#     kubernetes.io/ingress.class: azure/application-gateway
#     appgw.ingress.kubernetes.io/ssl-redirect: "true"
# spec:
#   rules:
#   - host: your-domain.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: k8s-azure-app-service
#             port:
#               number: 80
